spring:
  application:
    name: pix-boleto-kotlin
  datasource:
    url: jdbc:postgresql://localhost:5432/payments
    username: payments
    password: payments
  flyway:
    enabled: true
    locations: classpath:db/migration
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

logging:
  pattern:
    console: "%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX} level=%5p service=pix-boleto correlation_id=%X{correlationId} logger=%logger{36} message=%msg%n"

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus

payments:
  security:
    api-key: local-dev-key

resilience4j:
  circuitbreaker:
    instances:
      bank-rail-provider:
        sliding-window-type: COUNT_BASED
        sliding-window-size: 20
        minimum-number-of-calls: 10
        failure-rate-threshold: 50
        wait-duration-in-open-state: 30s
        permitted-number-of-calls-in-half-open-state: 5
  retry:
    instances:
      bank-rail-provider:
        max-attempts: 3
        wait-duration: 1s
        enable-exponential-backoff: true
        exponential-backoff-multiplier: 2
